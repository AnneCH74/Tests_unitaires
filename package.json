{
  "devDependencies": {
    "@vitest/coverage-v8": "^0.34.6",
    "@vitest/ui": "^0.34.6",
    "vitest": "^0.34.6"
  },
  "scripts": {
    "vitest": "vitest --ui",
    "coverage": "vitest run --coverage"
  }
}
